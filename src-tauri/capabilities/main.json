{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Permisos de la ventana principal, uno por uso real del frontend: eventos (listen/unlisten), el diálogo de abrir archivo, comprobar/descargar/instalar actualizaciones y relanzar tras instalar. Cada comando de la app pasa por invoke, que no necesita permiso.",
  "windows": ["main"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "dialog:allow-open",
    "updater:allow-check",
    "updater:allow-download",
    "updater:allow-install",
    "process:allow-restart"
  ]
}
